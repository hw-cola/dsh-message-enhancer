// @dsh/message-enhancer - host half.
//
// The plugin is client-only. The host entry keeps it compatible with the Web
// profile; the conversation index, plan review card, and plan history card
// all live in lib/client.js.

export const name = "message-enhancer";

export function apply() {}
